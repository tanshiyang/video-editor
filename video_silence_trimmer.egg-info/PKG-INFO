Metadata-Version: 2.4
Name: video-silence-trimmer
Version: 0.1.0
Summary: 自动检测并切除视频中的静音片段
Home-page: 
Author: 
Author-email: 
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: librosa>=0.10.0
Requires-Dist: numpy>=1.24.0
Requires-Dist: soundfile>=0.12.0
Requires-Dist: click>=8.0.0
Requires-Dist: loguru>=0.7.0
Dynamic: requires-python

# 视频静音切除工具 (Video Silence Trimmer)

## 1. 项目概述

### 1.1 目标
实现一个命令行工具/库，对视频文件进行音频分析，自动检测并切除视频中没有声音（静音）的片段，保留有音频的部分。

### 1.2 核心功能
- **音频分析**：提取视频音轨，检测静音与非静音区间
- **智能剪切**：按静音区间切除视频片段
- **灵活配置**：支持自定义静音阈值、最小静音时长等参数
- **多格式支持**：支持常见视频格式（MP4、MKV、MOV、AVI等）

### 1.3 应用场景
- 去除会议/访谈录像中的长时间停顿
- 清理屏幕录制中的无声空白
- 精简教学/演示视频
- 播客/配音素材预处理

---

## 2. 技术方案

### 2.1 技术栈
| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **核心语言** | Python 3.10+ | 生态丰富，适合音视频处理 |
| **视频处理** | FFmpeg (via ffmpy2/subprocess) | 业界标准，编解码支持全面 |
| **音频分析** | librosa / pydub | 音频特征提取、静音检测 |
| **项目封装** | click | 命令行界面 |
| **配置管理** | pydantic / dataclasses | 参数校验与配置建模 |
| **日志** | loguru | 简洁日志输出 |

### 2.2 依赖关系
```
ffmpeg (系统安装)
librosa >= 0.10.0
pydub >= 0.25.0
numpy >= 1.24.0
click >= 8.0.0
loguru >= 0.7.0
```

### 2.3 Windows 兼容性

本项目设计为跨平台运行，支持 Windows 10/11 系统。以下是 Windows 平台的具体兼容措施：

#### 2.3.1 FFmpeg 安装与配置

**安装方式（任选其一）：**

| 方式 | 命令 | 说明 |
|------|------|------|
| winget（推荐） | `winget install ffmpeg` | 自动配置 PATH |
| Chocolatey | `choco install ffmpeg` | 需要 Chocolatey |
| 手动安装 | 下载 zip 包 | 需手动配置 PATH |

**手动安装步骤：**
1. 下载 https://www.gyan.dev/ffmpeg/builds/ 或 https://ffmpeg.org/download.html
2. 解压到 `C:\ffmpeg`（或其他目录）
3. 将 `C:\ffmpeg\bin` 添加到系统 PATH
4. 验证：`ffmpeg -version`

**FFmpeg 环境检测代码：**
```python
import shutil

def check_ffmpeg():
    """检测 FFmpeg 是否可用"""
    ffmpeg_path = shutil.which("ffmpeg")
    if ffmpeg_path is None:
        raise FFmpegNotFoundError(
            "FFmpeg 未安装或未配置 PATH。\n"
            "Windows 安装方式：\n"
            "  方式1: winget install ffmpeg\n"
            "  方式2: 下载 https://ffmpeg.org/download.html 并配置 PATH"
        )
    return ffmpeg_path
```

#### 2.3.2 路径处理规范

**问题**：Windows 路径使用反斜杠 `\`，与 Linux/macOS 不兼容。

**解决方案**：全程使用 `pathlib.Path` 或在拼接路径时使用 `os.path.join`。

```python
from pathlib import Path
import tempfile
import os

# ✅ 正确写法
output_dir = Path(tempfile.gettempdir()) / "video_trimmer"
output_dir.mkdir(parents=True, exist_ok=True)

# ❌ 避免写法
output_dir = tempfile.gettempdir() + "\\video_trimmer"  # 可移植性差

# FFmpeg 命令中的路径（Windows 需要引号）
# 使用 .as_posix() 或手动加引号
video_path_str = str(video_path).replace("\\", "/")  # 传给 FFmpeg 用正斜杠
```

#### 2.3.3 子进程调用

```python
import subprocess

def run_ffmpeg(cmd: list, ...):
    """Windows 兼容的 FFmpeg 调用"""
    # Windows 下 subprocess 对中文路径处理有问题时使用 shell=True
    # 或先将路径转为短路径（8.3格式）
    try:
        result = subprocess.run(
            cmd,
            check=True,
            capture_output=True,
            text=True,
            encoding='utf-8',  # Windows 默认 GBK，显式指定 utf-8
        )
    except subprocess.CalledProcessError as e:
        # 捕获 FFmpeg 错误输出
        error_msg = e.stderr
        raise VideoProcessingError(f"FFmpeg 失败: {error_msg}")
```

#### 2.3.4 临时文件管理

```python
import tempfile
import shutil
from pathlib import Path

class TempDir:
    """跨平台临时目录管理"""
    def __init__(self):
        self.path = Path(tempfile.gettempdir()) / f"video_trimmer_{os.getpid()}"
        self.path.mkdir(parents=True, exist_ok=True)

    def __enter__(self):
        return self.path

    def __exit__(self, exc_type, exc_val, exc_tb):
        # Windows 文件锁释放需要稍作延迟
        import time
        time.sleep(0.1)
        shutil.rmtree(self.path, ignore_errors=True)
```

#### 2.3.5 编码与文本处理

| 问题 | 解决方案 |
|------|----------|
| 中文路径 | 使用 `Path` 对象，传给 FFmpeg 前转正斜杠字符串 |
| 控制台输出乱码 | 设置环境变量 `PYTHONIOENCODING=utf-8` |
| FFmpeg 输出编码 | `subprocess.run(encoding='utf-8')` 指定 |
| 日志文件 | loguru 默认支持 UTF-8，无需额外配置 |

#### 2.3.6 Windows 特定配置

```python
# config.py
import platform
import os

def get_default_config() -> TrimmerConfig:
    config = TrimmerConfig(...)

    # Windows 特定优化
    if platform.system() == "Windows":
        # Windows 临时文件放用户目录，避免 C盘权限问题
        config.temp_dir = Path(os.environ.get("TEMP", "~"))
        # FFmpeg 并发参数 Windows 建议降低
        config.ffmpeg_threads = 4

    return config
```

#### 2.3.7 常见问题与解决

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| `ffmpeg not found` | PATH 未配置 | 重启终端或手动添加 PATH |
| 中文路径报错 | 编码问题 | 使用 `pathlib` + `.as_posix()` |
| 临时文件无法删除 | 文件被占用 | 添加延迟 `time.sleep(0.1)` 后重试 |
| 内存不足 | 视频文件过大 | 分段处理，降低 `analysis_chunk_size` |
| 权限拒绝 | 输出目录无写权限 | 切换到用户可写目录如桌面 |

#### 2.3.8 测试验证清单

Windows 环境下部署前需验证：

- [ ] `ffmpeg -version` 命令行可用
- [ ] Python 能正常 import librosa
- [ ] 包含中文路径的视频文件能正常处理
- [ ] 临时文件能正常创建和删除
- [ ] 输出文件能正常写入（非只读目录）

---

## 3. 系统架构

### 3.1 模块划分

```
video_silence_trimmer/
├── __init__.py
├── cli.py              # 命令行入口
├── core/
│   ├── __init__.py
│   ├── analyzer.py      # 音频分析模块（静音检测）
│   ├── cutter.py        # 视频剪切模块
│   └── segment.py       # 片段模型定义
├── utils/
│   ├── __init__.py
│   ├── ffmpeg_utils.py  # FFmpeg 封装工具
│   └── audio_utils.py   # 音频处理工具
└── config.py            # 配置模型定义
```

### 3.2 核心流程

```
输入视频
    │
    ▼
┌─────────────────┐
│  提取音频流       │
│  (FFmpeg)        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  音频分析        │
│  (librosa)      │
│  - 计算响度      │
│  - 检测静音区间  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  生成剪切清单    │
│  (非静音片段)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  视频剪切合并    │
│  (FFmpeg concat) │
└────────┬────────┘
         │
         ▼
输出视频
```

---

## 4. 核心算法

### 4.1 静音检测算法

**步骤 1：音频特征提取**
```python
# 使用 librosa 加载音频，提取 RMS 能量
y, sr = librosa.load(audio_path, sr=16000)
rms = librosa.feature.rms(y=y)[0]
```

**步骤 2：帧级别判断**
```python
# 每帧能量低于阈值 → 标记为静音
is_silent = rms < SILENCE_THRESHOLD  # 默认: -40 dB
```

**步骤 3：连续静音区间合并**
```python
# 连续静音帧 >= 最小静音时长 → 确认为静音区间
# 合并相邻静音区间
```

### 4.2 参数定义

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `silence_threshold` | -40 dB | 静音判定阈值（能量低于此值判定为静音） |
| `min_silence_duration` | 0.5 s | 最小静音持续时间，短于此值不切除 |
| `keep_before_silence` | 0.1 s | 静音前保留的缓冲时间（避免截断感） |
| `keep_after_silence` | 0.1 s | 静音后保留的缓冲时间 |

### 4.3 静音区间合并规则
```
原始区间: [0-2s有声] [2-3s静音] [3-4s静音] [4-5s有声]
          [5-6s静音] [6-10s静音] [10-11s有声]

合并后:   [0-2s有声] [2-6s静音] [6-10s静音] [10-11s有声]
          (相邻静音区间合并)

最终剪切: 保留 [0-2s], [10-11s] 两个片段
```

---

## 5. API 设计

### 5.1 命令行接口

```bash
video-trimmer trim <input_video> [OPTIONS]

Options:
  -o, --output PATH              输出文件路径
  -t, --threshold DB             静音阈值(dB)，默认 -40
  -d, --min-duration SEC         最小静音时长(s)，默认 0.5
  --keep-before SEC              静音前保留时间，默认 0.1
  --keep-after SEC               静音后保留时间，默认 0.1
  -v, --verbose                  输出详细日志
  --dry-run                      仅分析，不执行剪切
```

**示例：**
```bash
# 基本用法
video-trimmer trim recording.mp4 -o output.mp4

# 自定义阈值
video-trimmer trim recording.mp4 -o output.mp4 -t -50 -d 1.0

# 预览模式（仅分析）
video-trimmer trim recording.mp4 --dry-run -v
```

### 5.2 编程 API

```python
from video_silence_trimmer import VideoTrimmer, TrimmerConfig

config = TrimmerConfig(
    silence_threshold_db=-40,
    min_silence_duration=0.5,
    keep_before=0.1,
    keep_after=0.1,
)

trimmer = VideoTrimmer(config)
result = trimmer.trim("input.mp4", "output.mp4")

print(f"原始时长: {result.original_duration:.2f}s")
print(f"输出时长: {result.output_duration:.2f}s")
print(f"切除片段数: {len(result.removed_segments)}")
```

### 5.3 返回结果模型

```python
@dataclass
class TrimResult:
    original_duration: float      # 原始视频时长(秒)
    output_duration: float        # 输出视频时长(秒)
    removed_segments: List[Segment]  # 被切除的片段列表
    kept_segments: List[Segment]     # 保留的片段列表
    processing_time: float          # 处理耗时(秒)

@dataclass
class Segment:
    start: float   # 起始时间(秒)
    end: float     # 结束时间(秒)
    is_silent: bool  # 是否为静音片段
```

---

## 6. 关键实现细节

### 6.1 音频提取
```python
def extract_audio(video_path: str, audio_path: str):
    """使用 FFmpeg 提取音频流"""
    cmd = [
        "ffmpeg", "-y", "-i", video_path,
        "-vn", "-acodec", "pcm_s16le",
        "-ar", "16000", "-ac", "1",
        audio_path
    ]
    subprocess.run(cmd, check=True, capture_output=True)
```

### 6.2 视频剪切
```python
def cut_and_concat(video_path: str, segments: List[Segment], output_path: str):
    """使用 FFmpeg concat 模式合并片段"""
    # 方案: 生成临时文件列表，用 concat demuxer 合并
    with tempfile.NamedTemporaryFile(mode='w', suffix='.txt') as f:
        for seg in segments:
            f.write(f"file '{seg.filename}'\n")
        # ffmpeg -f concat -safe 0 -i list.txt -c copy output.mp4
```

### 6.3 分段处理策略
对于大文件或需要保持质量的场景：
1. 将每个非静音片段单独导出为临时文件
2. 使用 FFmpeg concat 协议合并
3. 清理临时文件

---

## 7. 错误处理

| 场景 | 处理策略 |
|------|----------|
| 文件格式不支持 | 抛出 `UnsupportedFormatError`，提示支持的格式 |
| FFmpeg 未安装 | 抛出 `FFmpegNotFoundError`，附带安装指引 |
| 视频无音频流 | 抛出 `NoAudioStreamError`，建议用户检查视频 |
| 磁盘空间不足 | 在写入前检查可用空间，不足时提前终止 |
| 权限拒绝 | 捕获并提示输出路径权限问题 |

---

## 8. 性能考量

| 优化点 | 方案 |
|--------|------|
| 音频分析 | librosa 支持降采样分析（16000Hz 足够） |
| 内存占用 | 分帧处理，不一次性加载全部音频到内存 |
| 大文件 | 分段处理 + concat，避免内存溢出 |
| 并行化 | 多视频批量处理时可并行（未来扩展） |

---

## 9. 扩展方向

- [ ] **GUI 界面**：tkinter/pyqt 图形化工具
- [ ] **批量处理**：支持文件夹批量处理
- [ ] **预览模式**：在剪切前预览要切除的区间
- [ ] **过渡效果**：切除点自动添加淡入淡出
- [ ] **字幕联动**：根据静音区间自动调整字幕时间轴
- [ ] **Web 服务**：提供 REST API 服务

---

## 10. 文件清单

```
video_silence_trimmer/
├── DESING.md              # 本文档
├── README.md              # 使用说明
├── requirements.txt       # Python 依赖
├── setup.py               # 包安装配置
├── pyproject.toml         # 项目元数据
├── video_silence_trimmer/
│   ├── __init__.py
│   ├── __main__.py        # python -m 入口
│   ├── cli.py
│   ├── config.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── analyzer.py
│   │   ├── cutter.py
│   │   └── segment.py
│   └── utils/
│       ├── __init__.py
│       ├── ffmpeg_utils.py
│       └── audio_utils.py
└── tests/
    ├── __init__.py
    ├── test_analyzer.py
    ├── test_cutter.py
    └── test_integration.py
```

---

## 11. 开发计划

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| **Phase 1** | 项目初始化、FFmpeg 封装、基础音频分析 | P0 |
| **Phase 2** | 静音检测算法实现、片段模型 | P0 |
| **Phase 3** | 视频剪切与合并逻辑 | P0 |
| **Phase 4** | CLI 接口完善、dry-run 模式 | P1 |
| **Phase 5** | 错误处理、日志、单元测试 | P1 |
| **Phase 6** | 文档、示例、性能优化 | P2 |

---

*文档版本: v1.0*
*最后更新: 2026-04-15*
